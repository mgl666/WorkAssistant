# 复制为 .env.server，并与 PostgreSQL 中创建的账号保持一致。
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=work_assistant
PGUSER=work_assistant
PGPASSWORD=replace-with-your-postgresql-password
PORT=3100
HOST=127.0.0.1
# 通过 http://服务器IP:190 访问时必须为 false；启用 HTTPS 后改为 true。
COOKIE_SECURE=false
SESSION_DAYS=30
